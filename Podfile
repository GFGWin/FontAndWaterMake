# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FontAndWaterMake' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'GPUImage', '~> 0.1.7'
  # Pods for FontAndWaterMake

  target 'FontAndWaterMakeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FontAndWaterMakeUITests' do
    # Pods for testing
  end

end
